name: Microservice A CI/CD

on:
  push:
    paths:
      - "apps/ms-a/**"
  pull_request:
    paths:
      - "apps/ms-a/**"

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: |
          cd apps/ms-a
          npm install

      - name: Run Unit Tests
        run: |
          cd apps/ms-a
          npm test

      - name: Build Docker Image
        run: |
          cd apps/ms-a
          docker build -t ghcr.io/${{ github.repository }}/ms-a:${{ github.sha }} .

      - name: Smoke Test Container
        run: |
          docker run -d --name smoke-test -p 3000:3000 ghcr.io/${{ github.repository }}/ms-a:${{ github.sha }}
          sleep 5
          curl --fail http://localhost:3000/health || (docker logs smoke-test && exit 1)
          docker stop smoke-test && docker rm smoke-test

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to GHCR
        if: github.event_name != 'pull_request'
        run: |
          docker push ghcr.io/${{ github.repository }}/ms-a:${{ github.sha }}
          docker tag ghcr.io/${{ github.repository }}/ms-a:${{ github.sha }} ghcr.io/${{ github.repository }}/ms-a:latest
          docker push ghcr.io/${{ github.repository }}/ms-a:latest
